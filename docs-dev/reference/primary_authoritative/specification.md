# Specification

**Version:** 0.0 (placeholder) · **Last updated:** 2026-06-02

> **This is a minimal placeholder, not the binding specification.**
>
> The real binding specification for PocketGaze has **not yet been written**. It is
> planned as a later prompt, drafted in
> `docs-dev/agent/prompts/002_DRAFT_create_specification.md`. Until that prompt runs and
> fills the sections below, this document imposes **no binding constraints** — every
> section is "not yet decided".
>
> When the binding specification is created, it must capture at least:
>
> - PocketGaze's **portfolio purpose** (showing how smartphone-camera eye tracking can be
>   implemented in practice); and
> - the **master "Show implementation details" control** for revealing/hiding the optional
>   implementation/subprocess panels on each step page.
>
> Background context for that work lives in (non-binding):
> `docs-dev/reference/secondary_background/overview.md` and
> `docs-dev/reference/secondary_background/smartphone_eye_tracking_background.md`.

When the codebase and this document conflict, this document is correct. An empty or
stubbed section means "not yet decided", not a constraint. Bump the version when a real
decision is recorded.

## Scope

_Not yet decided. See note above._

## Architecture

_Not yet decided. See note above._

## Data schemas

_Not yet decided. See note above._

## Domain rules

_Not yet decided. See note above._

## Naming and voice

_Not yet decided. British English for user-facing text is recorded as a project
convention in `AGENTS.md` until the binding specification supersedes it._

## Locked decisions

_None yet. See note above._
